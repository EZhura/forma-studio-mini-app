FORMA Studio — Project Brief SAFE Update v2

This archive restores the complete working brief and adds visible validation.

Replace only:
- templates/index.html
- static/css/style.css
- static/js/brief.js

Important:
Do not edit valid() manually. Replace the three files from this archive.

Added:
- highlighted missing question or input;
- automatic scroll to the first missing answer;
- focus on the missing input;
- specific field title in the error message;
- error highlight disappears after the user answers.
