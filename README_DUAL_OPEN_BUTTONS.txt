FORMA Studio — dual open buttons

Replace:
- bot.py

Added to /start, /brief and /projects responses:
- Web App button — opens inside Telegram;
- Open in browser — opens the same HTTPS URL in an external browser.

After git push, Render will redeploy automatically.
The existing webhook does not need to be registered again unless it was removed.
